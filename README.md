# entity-kit
Headless React component library implementing the BeanInfo pattern. Define an EntityDescriptor for any object type, render it as list, tile grid, detail view, or trash — fully themeable via CSS custom properties.
